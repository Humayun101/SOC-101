[[02. Creating a Malware For Testing]]


- Starting from network connections help us narrow down the scope

**Network Connection Utilities**

> net view is the cli utility to display **a list of domains, computers, or shared resources** (such as file shares and printers) that are available on the network
> 	net view //127.0.0.1 > data ex filtration point adversaries use
> 		we can also see the share resources from other IPs
> 	net share > list out shared resources going from our local computer
> 	 net session/ net use> shows mapping of mounted shared resources attacker can create

- net stat 
	- -a: shows all active tcp connections 
	- -n: IP and ports are displayed numerically and not DNS resolved
	- -o : show  process id
	- -b : actual file name of process
		- State:Listening is open to connection
		- State: Established is the connection that are remote connection to system

*tcp view:  GUI Utility to give us more informative view of net stat*
- Part of sys internal toolsets
- https://learn.microsoft.com/en-us/sysinternals/downloads/tcpview
